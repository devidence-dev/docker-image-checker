{
    "name": "Docker Image Checker Development",
    "dockerComposeFile": [
        "docker-compose.yml"
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "golang.go"    
            ]
        }
    },
    "service": "docker-image-checker",
    "workspaceFolder": "/app",
    "remoteUser": "root"
}